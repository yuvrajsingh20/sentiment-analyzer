# ─────────────────────────────────────────────────────────────────────────────
# Authentication (login screen)
# ─────────────────────────────────────────────────────────────────────────────
# Credentials for the demo login screen.
AUTH_USERNAME=analyst
AUTH_PASSWORD=change-me

# Secret used to sign the session cookie (HMAC-SHA256).
# Generate one with:  openssl rand -base64 32
AUTH_SECRET=replace-with-a-long-random-string

# Session lifetime in seconds (default 43200 = 12h)
SESSION_TTL_SECONDS=43200

# Optional Google sign-in. Client ID alone is enough for Continue with Google.
# In Google Cloud Console → Credentials, on the OAuth client add:
#   Authorized JavaScript origin:  http://localhost:3000
#   Authorized redirect URI:       http://localhost:3000/api/auth/callback/google
# GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback/google


# ─────────────────────────────────────────────────────────────────────────────
# Orchestration: n8n  (UI → n8n → Gemini)
# ─────────────────────────────────────────────────────────────────────────────
# Production webhook URL of the imported n8n workflow. REQUIRED for analysis.
# Next.js never calls Gemini; the API key lives in n8n credentials.
# For local plumbing without n8n: npm run n8n:simulate, then point this at it.
# N8N_WEBHOOK_URL=https://your-n8n-host/webhook/sentiment-analyze

# Shared secret sent as the `x-api-key` header to the n8n webhook.
# Must match N8N_WEBHOOK_SECRET in the n8n environment.
# N8N_WEBHOOK_SECRET=replace-with-a-long-random-string

# Milliseconds to wait for n8n before failing (default 120000)
# N8N_TIMEOUT_MS=120000


# ─────────────────────────────────────────────────────────────────────────────
# AI provider (n8n credential, or local simulator stand-in)
# ─────────────────────────────────────────────────────────────────────────────
# Real n8n: Header Auth named "Gemini API", name `x-goog-api-key`.
# Next.js does not call Gemini and should not need this key.
#
# `npm run n8n:simulate` reads GEMINI_API_KEY from .env.local and makes the
# real generateContent call. Leave it unset to stub Gemini.
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-flash-latest


# ─────────────────────────────────────────────────────────────────────────────
# MongoDB Atlas (analysis history)
# ─────────────────────────────────────────────────────────────────────────────
# When set, previous analyses are stored in Atlas instead of data/history/.
# Add your current IP (or 0.0.0.0/0 for deploy) under Network Access.
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net
# MONGODB_DB=sentiment-analyzer
